# Phase B.1 — the test player every capture uploads as (Decision 2).
# A real Player row with this id must exist in the backend DB, or the upload
# 404s. Seed one before Section 5 (see README.md). Copy this file to `.env`
# (git-ignored) and change the id if your seeded player isn't 1.
VITE_REF_PLAYER_ID=1
