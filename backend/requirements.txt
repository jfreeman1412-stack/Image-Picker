fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12

# CV / ML
insightface==0.7.3
onnxruntime-gpu==1.19.2  # Phase 8: GPU acceleration. Requires CUDA 12.x runtime + cuDNN 9.x. Falls back to CPU automatically if CUDA can't init (see face_detector.py).
opencv-python-headless==4.10.0.84
numpy==1.26.4
scikit-learn==1.5.2
Pillow==10.4.0
exifread==3.0.0
# Phase 4.3: replaced Haar smile cascade with FER (small pretrained CNN).
# Pulls in tensorflow as a transitive dep — large install but accurate.
fer==22.5.1

# Storage
SQLAlchemy==2.0.35

# Dev
pytest==8.3.3
httpx==0.27.2
